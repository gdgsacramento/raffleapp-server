{
    "rules": {
        ".read": true,
        ".write": true,
        "raffles": {
            "$id": {
                "participants": {
                    "$id": {
                        "name": {
                            ".validate": "newData.isString() && newData.val().length < 30"
                        }
                    }
                }
            }
        }
    }
}
