{
  "rules": {
    ".read": true,
    ".write": true,
    "raffles": {
      "$id": {
        "participants": {
          "$participant": {
            ".validate": "newData.isString() && newData.val().length < 30"
          }
        }
      }
    }
  }
}
